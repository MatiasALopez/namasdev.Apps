@using namasdev.Apps.Web.Portal.Helpers;
@model namasdev.Apps.Entidades.Entidad
@{
    var namespaces = new
    {
        Aplicacion = Model.AplicacionVersion.Aplicacion.Nombre
    };

    var clases = new
    {
        ClaseBase =
            Model.PropiedadesDefault.IDPropiedadTipoId.HasValue
            ? FormatoHelper.ClaseGenerica("ParametrosEntidadBase", Model.PropiedadesDefault.IDTipo.CLRType)
            : string.Format("ParametrosConUsuarioBase"),
    };
}
using System;

namespace @(namespaces.Aplicacion).Negocio.DTO.@(Model.NombrePlural)
{
    public class DesmarcarComoBorradoParametros : @Html.Raw(clases.ClaseBase)
    {
    }
}
